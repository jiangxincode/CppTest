#include <stdio.h>

/** \brief 大数相减
 *
 * \param 被减数字符串的指针，要求为正整数
 * \param 减数字符串的指针，要求为正整数
 * \param 指向结果字符串的指针
 * \return 指向结果字符串的指针
 *
 */
/*
char* big_diff(const char *str1, const char *str2, char *result)
{
    char pstr1[BUFSIZ];
    char pstr2[BUFSIZ];

    strcpy(pstr1,str1);
    strcpy(pstr2,str2);

    int len1 = strlen(pstr1);
    int len2 = strlen(pstr2);

    for(int i=len2-1;i>=0;i--)
    {
        if(pstr1[i]<pstr2[i])
        {
            int j = i-1;
            while(j>=0 && pstr2[j]==0)
            {
                pstr[j] = '9'
            }
        }
    }


}
int main0023()
{
    str1 = "3445332757274298572937520957814";
    str2 = "938756532757238953268429320";
    return 0;
}
*/
